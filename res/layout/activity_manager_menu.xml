<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.uttara.gtmanager.ManagerMenuActivity" >

    <Button
        android:id="@+id/members_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/projects_btn"
        android:layout_below="@+id/projects_btn"
        android:layout_marginTop="16dp"
        android:onClick="membersList"
        android:text="@string/members_btn_lbl" />

    <Button
        android:id="@+id/projects_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="81dp"
        android:onClick="projectsMenu"
        android:text="@string/project_btn_lbl" />

    <Button
        android:id="@+id/tasks_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/members_btn"
        android:layout_below="@+id/members_btn"
        android:layout_marginTop="19dp"
        android:onClick="taskMenu"
        android:text="@string/task_btn_lbl" />
		
</RelativeLayout>
